# Placeholder secrets used solely so `esphome config examples/*.yaml`
# can parse the example configs in CI. Real deployments should keep
# their own secrets.yaml outside this repository.
wifi_ssid: "example-wifi"
wifi_password: "example-password"
mqtt_broker: "mqtt.example.invalid"
mqtt_user: "example"
mqtt_password: "example"
