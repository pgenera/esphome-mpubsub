esphome:
  name: pubsub-subscriber

host:

logger:
  level: VERBOSE

external_components:
  - source:
      type: local
      path: ../components
    components: [mpubsub]

mpubsub:
  port: 18512
  scope: link-local
  hops: 1
  on_message:
    - topic: "test/control"
      then:
        - logger.log:
            format: "received control message of %u bytes"
            args: ['x.size()']

sensor:
  - platform: mpubsub
    topic: "test/temp"
    name: "Subscribed Temperature"
    mode: subscribe
    accuracy_decimals: 1
