esphome:
  name: pubsub-subscriber-enc

host:

logger:
  level: VERBOSE

external_components:
  - source:
      type: local
      path: ../components
    components: [mpubsub]

mpubsub:
  port: 18512
  scope: link-local
  hops: 1
  encryption:
    key: "shared-test-secret"

sensor:
  - platform: mpubsub
    topic: "test/temp"
    name: "Subscribed Temperature"
    mode: subscribe
    accuracy_decimals: 1
